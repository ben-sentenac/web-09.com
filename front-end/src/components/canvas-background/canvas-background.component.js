import { useEffect, useRef,useState } from "react";



const Canvas = () => {

    const ref = useRef();

    useEffect(() => {

        


    })
    return (
        <canvas ref={ref} id="canvas"></canvas>
    );
};